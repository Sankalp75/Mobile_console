# ─── Core (all platforms) ────────────────────────────
websockets>=12.0

# ─── Linux: virtual gamepad via uinput ───────────────
evdev>=1.6; sys_platform == "linux"

# ─── Windows: virtual gamepad via ViGEmBus ───────────
vgamepad>=0.3.0; sys_platform == "win32"
